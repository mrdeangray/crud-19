import React from "react";
import { Link } from "react-router-dom";

const ReadLocations = () => {
  return (
    <div>
        <Link to={`/`}>Back</Link>
      <h1>ReadLocations</h1>
    </div>
  );
};

export default ReadLocations;
